# このリポジトリで作業する際の規則

対象は C# / .NET Framework 4.8 の小規模なコンソールアプリです。

1. 変更前に [開発規則](docs/development-rules.md) と [現行仕様](docs/specification.md)、`git status --short` を確認する。
2. ユーザーの依頼を作業範囲とする。既存の未コミット変更を勝手に破棄せず、無関係な整形・改名・設計変更を混ぜない。
3. 既存のソリューションとビルド方式を維持する。ファイルを追加・移動したら `.csproj` の明示的な `Compile` 登録も更新する。
4. 小さなメソッドと明確な引数を優先する。層・インターフェース・DI コンテナを形式的に導入しない。
5. リファクタリングでは入出力・牌表現・役番号・乱数の流れを維持する。仕様変更や不具合修正は、その違いと期待結果を明記する。
6. 共通処理の抽出後は全呼び出し元と実際の起動経路を確認する。新クラスを作っただけで移行完了としない。
7. コード変更はビルドと変更に対応した検証を行う。未実行・既存の失敗・今回の失敗を分けて報告する。文書のみの変更にアプリの全テストは要求しない。
8. コミットや公開は依頼された範囲で行う。複数の作業が競合する場合に worktree を利用し、単独作業に作成を必須としない。

規則は新規コードと変更箇所に適用する。既存コード全体を一度に規則へ合わせる必要はない。仕様書にある未検証事項を、検証済みの機能として扱わない。

`../furmah` と `../furmah-codex` は参考資料であり、このリポジトリの規則を継承する親ディレクトリではない。Beacon、Web 配備、外部レビュー、即時コミット等の運用は本プロジェクトの必須条件にしない。
