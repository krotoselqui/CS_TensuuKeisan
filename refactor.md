# リファクタリング前後の動作互換性

この文書は、`Program.cs` の責務を分離したリファクタリングについて、整理前と整理後で利用者から見える動作が変わらないことを説明する。対象はコードの配置、型やメソッドの分割、呼び出し経路の置き換えであり、麻雀の役判定・点数計算・問題生成ルールを変更するものではない。

## 維持した動作

### 入力と起動の流れ

起動後にタイトルを表示し、問題数、シード、表示形式を順に入力する流れは変わらない。不正な文字列や範囲外の値を再入力する条件も従来どおりである。`Main` の処理を `ReadOptions` や表示用メソッドへ分離したが、入力順と受理する値は維持している。

### 問題生成

`Program.GenerateProblems` が従来と同じ役の重みを使って役を抽選し、`makeYaku` が通常形・七対子・国士無双・九蓮宝燈系の問題を生成する。面子生成は `MeldGenerator` に移したが、順子、刻子、槓子、雀頭の各メソッドは同じ条件分岐、同じ残り牌配列、同じ共有乱数源 `Program.r` を使う。

固定シードでは乱数の呼び出し順、呼び出し回数、抽選範囲を変えていない。そのため、同じ問題数・シード・表示形式を指定した場合、問題ファイル、解答ファイル、役別集計の内容は整理前の基準出力と一致する。

### 採点

同じ手牌を東家ツモ、東家ロン、西家ツモ、西家ロンの4条件で評価する流れは変わらない。`HandEvaluator` の役判定、符・飜計算、役満判定、最高点候補の選択は変更せず、`ScoreResult` などのデータ型も同じ契約を維持している。

### 出力と終了処理

`question.txt`、`answer.txt`、`answer_yaku.txt` を実行時のカレントディレクトリに Shift_JIS で保存する。ファイル形式、上書き動作、進捗表示、最後のキー入力待ちは従来どおりである。出力処理を `FormatPayment`、`FormatDetails`、`WriteAnswers` へ分離しても、文字列の構成と出力順は変更していない。

## 互換性を確認した方法

変更前の基準出力を固定し、次の条件で変更後の実行結果を比較している。

| 検査対象 | 条件 | 確認内容 |
| --- | --- | --- |
| 問題生成 | シード `0`、`12345` × 表示形式 `0`、`1` | 各20問の3ファイルとコンソール出力を比較 |
| 採点 | 固定された通常形、七対子、国士無双、九蓮宝燈、副露、槓、親子ツモロン | 役、符、飜、支払額、役満倍率を比較 |
| 牌変換 | 境界を含む6ケース | 物理牌ID、牌種、色、数字、幺九牌判定を比較 |
| 実行経路 | Debug／Release | `Main`、問題生成、入力エラー、進捗、例外時の後始末を確認 |

現在の回帰検査は、採点7ケース、牌変換6ケース、生成・入力・例外検査142アサーションを両構成で通過している。`Check-Repository.ps1` により、`.csproj` のソース登録と文書リンクも確認している。

## 変更していない範囲

この互換性確認は、リファクタリングによる既存動作の保持を対象とする。麻雀ルール上の完全な正しさ、新しい役の追加、役の抽選分布の改善、EOF時の入力終了、カーソル描画の実画面挙動までは保証しない。これらを変更する場合は、リファクタリングとは別の仕様変更として扱い、新しい期待値と検査を追加する。

